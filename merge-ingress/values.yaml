ingressClass: merge
mergedIngressClass: alb

image:
  repository: jakubkulhan/merge-ingress
  tag: latest
  pullPolicy: Always

rbac:
  create: true

serviceAccount:
  # Specifies whether a ServiceAccount should be created
  create: true
  # The name of the ServiceAccount to use.
  # If not set and create is true, a name is generated using the fullname template
  name:

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}
